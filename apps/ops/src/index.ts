/**
 * apps/ops — order queue, image human-review, moderation, abuse reports, cost panel.
 *
 * SCAFFOLD ONLY. Owner: F3 — docs/SPEC.md §12.
 * Same boundary rule as apps/web: only packages/{contract,mock,ui,shared}.
 */
export const APP_NAME = 'kendihikayem-ops' as const;
